# تحديث v8.7 — الحذف المتبادل وتوحيد الترقيم والتكويد

## 1) الحذف بقى مسموع في الناحيتين
- اتعملت نواة حذف واحدة `deleteOrderCore` بيستخدمها زر الحذف في السيستم **و** الحذف الجاي
  من موقع البيع، فالخطوات واحدة بالظبط: استرجاع الورق والشنط واليدين، مسح المصروفات
  والدفعات وقيود الخزنة، وتسجيل الحركة في سجل النشاط.
- `pullStoreDeletions()` بتسحب كل أوردر اتحذف من لوحة تحكم الموقع وتحذفه هنا وتأكد الاستلام.
- الحذف من هنا لسه بيمسح من الموقع زي ما كان، بس من غير ما يرجّع الأمر لنفسه لو الحذف
  أصله جاي من الموقع.

## 2) اللغبطة في الأوردرات الشبح
`reconcileStoreOrders()` بتقارن أوردرات النظامين كل ~5 دقايق ومع كل «مزامنة الآن»:
- أوردر متعلّم على الموقع إنه «في السيستم» وهو مش موجود هنا ← بيتفك ربطه، فالموقع
  ما يفضلش رافض حذفه بحجة إنه مربوط بالمصنع. (ده كان سبب رسالة «لازم يتحذف من سيستم المصنع».)
- أوردر موجود هنا وناقص على الموقع ← بيترجع يتبعت تاني تلقائيًا.

## 3) الترقيم بقى واحد
- رقم الأوردر رقم ترتيبي، فلما أوردر قديم يتحذف كل اللي بعده بينزل رقمه.
  عمود جديد `store_synced_display_no` بيحفظ آخر رقم اتبعت للموقع، و`resyncStoreDisplayNumbers()`
  بتكتشف أي اختلاف وتبعت الترقيم الجديد فورًا (وبتشتغل تلقائيًا بعد أي حذف).
- الموقع بقى بيعرض **رقم المصنع فقط** وما بيرجعش لرقم الصف الداخلي في قاعدة البيانات
  (ده كان سبب 167 هنا و275 هناك).

## 4) تكويد CZ داخل السيستم
- كل أوردر له كود `CZ-YYMM-XXXX` (بيتحط تلقائيًا عند الإنشاء وبيتعمل backfill عند التشغيل).
- الكود ظاهر تحت رقم الأوردر في قائمة الأوردرات ومعاه زر نسخ، وكمان في مودال الدفعات
  علشان تسجيل العربون أو المبلغ الكامل يبقى على الأوردر الصح.
- البحث في قائمة الأوردرات بيلاقي بالكود أو رقم الأوردر أو اسم العميل،
  والكود بيتقارن من غير شرط الشرطات أو الحروف الكبيرة (`cz26081847` = `CZ-2608-1847`).
