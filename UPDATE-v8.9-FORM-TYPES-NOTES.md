# تحديث v8.9 — فلترة أنواع المنتجات ومقاسات الفورم حسب النوع

## 1) فلتر الأنواع في مكتبة الفورم (`forms.html`)

- اتشال السيلكت القديم "كل أنواع المنتجات" واتحط مكانه فلتر متعدد بالـ checkboxes بنفس تجربة **فلتر الحالات** في صفحة قائمة الأوردرات:
  - أزرار **✅ تحديد الكل** و **✖ مسح الكل**.
  - عداد بعدد الأنواع المختارة + سطر توضيحي أسفل القائمة.
  - كل نوع جنبه أيقونة وعدد الفورم المسجلة فيه.
  - القائمة بتتقفل لما تدوس بره، وبتتحدث تلقائيًا لما تفتح "الفورم المحذوفة".
- الفلتر شغال مع البحث النصي في نفس الوقت.

## 2) مقاسات الفورمة حسب نوع المنتج

الحقول الظاهرة في محرر الفورمة بقت تتغير حسب النوع المختار:

| النوع | الحقول |
|---|---|
| شنط | طول + عرض + **جنب / منفاخ** |
| علب بطاطس / باكيتات بطاطس / علب برجر | طول + عرض + **ارتفاع** |
| أكياس سندوتش | طول + عرض + **شكل الكعب** (بدون كعب / متكعب / كعب لطش)، ومقاس الكعب بيظهر بس لما يكون فيه كعب |
| كريب | **مقاس جاهز** (صغير / وسط / كبير) بدون أي أبعاد |
| أخرى | طول + عرض + ارتفاع (اختياري) |

- الحقول المخفية مش مطلوبة (required) فمش هتمنع الحفظ، وبتتصفّر تلقائيًا عند تغيير النوع.
- المقاس الإجمالي للفورمة (الطول والعرض) لسه مطلوب لكل الأنواع لأنه بيحدد التشغيل (ربع/نص/فرخ كامل).
- جدول المكتبة وشاشة التفاصيل بيعرضوا المقاس بالطريقة الصح لكل نوع.
- التحليل التلقائي (Gemini) بقى بيستخرج كمان مقاس الكريب وشكل كعب الأكياس.

### أعمدة جديدة في قاعدة البيانات

- جدول `forms`: `size_variant` (مقاس الكريب)، `gusset_style` (شكل كعب الأكياس).
- جدول `orders`: `product_type` (نوع منتج الأوردر، الافتراضي **شنط**)، و`form_size_variant` و`form_gusset_style` كنسخة محفوظة من بيانات الفورمة وقت الربط.

> الأعمدة بتتضاف تلقائيًا عند أول تشغيل. وكمان بيتعمل ترحيل تلقائي مرة واحدة:
> - الأوردر القديم المربوط بفورمة بياخد نوع الفورمة المحفوظة معاه، فالربط ما بيضيعش.
> - فورم الأكياس القديمة اللي كان الجنب متسجل فيها بتاخد شكل كعب "متكعب"، واللي بجنب صفر بتاخد "بدون كعب".

## 3) فلترة الفورم حسب نوع الأوردر

- في صفحة **أوردر جديد** (`add_order.html`) اتضاف حقل **نوع المنتج** في أول كل كارت صنف.
- قائمة **الفورمة المستخدمة** في مرحلة التكسير بتعرض فورم النوع المختار فقط؛ لو الأوردر شنط مش هتظهر أي فورمة كريب أو علب.
- لو مفيش فورم مسجلة للنوع ده بتظهر رسالة "لا توجد فورم مسجلة لنوع ...".
- تغيير النوع بيلغي أي فورمة مختارة من نوع تاني تلقائيًا.
- نفس الحاجة في **مودال تعديل الأوردر** و**إضافة صنف** في صفحة قائمة الأوردرات.
- السيرفر بيرفض حفظ أوردر مربوط بفورمة من نوع مختلف عن نوع الأوردر (حماية إضافية لو حد بعت البيانات يدويًا).
- نوع المنتج بيظهر في سطر تفاصيل الصنف في قائمة الأوردرات وفي فاتورة/تفاصيل الأوردر.
