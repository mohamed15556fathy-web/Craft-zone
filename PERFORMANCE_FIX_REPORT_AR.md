# تقرير إصلاح بطء تشغيل Craft Zone

التاريخ: 2026-08-27

## سبب البطء

- كان `ensureSchema()` يعيد فحص وترحيل كل الجداول والبيانات قبل أن يبدأ السيرفر في الاستماع.
- كان `runPostListenStartupMaintenance()` يعيد المصالحات المحاسبية والمخزنية الشاملة عند كل Restart باستخدام نفس اتصال SQLite الخاص بالصفحات.
- كان `/dashboard-data` يستدعي `syncAllSalesHistory()` في كل مرة تفتح فيها لوحة التحكم.
- كانت واجهة لوحة التحكم تطلب بيانات المخزون ثم النواقص بالتتابع وتعرض صفرًا أثناء الانتظار.

## التعديلات

### `server.js`

- إضافة جدول `schema_migrations` وإصدار سكيمة ثابت. الترحيل الكامل يعمل مرة واحدة فقط، ثم يستخدم السيرفر مسار Fast Start.
- الإبقاء على المصالحة الشاملة بعد استيراد Backup أو من خلال Health Check، وإزالتها من Startup العادي.
- تجميع مصالحة الاسترجاع في `runFullSystemReconciliation()` مع إزالة الاستدعاءات المكررة.
- تشغيل النسخ الاحتياطي والمزامنة الخارجية كمهام مؤجلة في الخلفية دون انتظارها قبل خدمة المستخدم.
- إزالة إعادة بناء `sales_history` من مسار لوحة التحكم.
- إزالة إعادة البناء من مسارات القراءة فقط في الحسابات والتقارير والتجار والشركاء والمرابحة وسجلات المبيعات؛ مع إبقائها في مسارات الإصلاح والتسويات والـRestore.
- إضافة `/dashboard-summary` لإرجاع أرقام اللوحة والنواقص والروابط السريعة في استجابة واحدة.
- الإبقاء على `/dashboard-data` و`/low-stock` للتوافق مع أي واجهات قديمة.

### `index.html`

- استعمال `/dashboard-summary` بدل طلبات متتابعة.
- إظهار علامة تحميل بدل أرقام صفرية مضللة.
- إظهار رسالة خطأ واضحة إذا تعذر تحميل البيانات.
- عرض اسم المستخدم والقائمة فورًا دون انتظار بيانات المخزون.

## نتائج القياس على قاعدة البيانات المصححة

| القياس | النتيجة |
| --- | ---: |
| أول تشغيل بعد إضافة إصدار السكيمة | 3966 ms |
| تسجيل الدخول في أول تشغيل | 86 ms |
| تحميل Dashboard Summary | 9 ms |
| التشغيل الطبيعي التالي Fast Start | 243 ms |
| تسجيل الدخول بعد Fast Start | 60 ms |
| تحميل Dashboard Summary بعد Fast Start | 11 ms |

اختبار Smoke لمسارات العرض بعد Fast Start:

| المسار | HTTP | الزمن المحلي |
| --- | ---: | ---: |
| Dashboard | 200 | 10 ms |
| Accounts Summary | 200 | 24 ms |
| Reports | 200 | 10 ms |
| Traders | 200 | 6 ms |
| Partners | 200 | 4 ms |
| Murabaha | 200 | 5 ms |
| Expenses | 200 | 5 ms |
| Sales Logs | 200 | 117 ms |

أول تشغيل ينفذ ترحيل الإصدار مرة واحدة. التشغيلات التالية هي الحالة الطبيعية على Railway طالما قاعدة البيانات موجودة على Volume دائم.

## اختبارات السلامة

- اختبارات الوحدات: 10/10.
- Regression لقاعدة البيانات: 17/17.
- بصمة الحسابات قبل وبعد التشغيل: `c66aeb51f77abdc7e15f8fe1b3e04ff57f3caea9c2677fdd124f786f89ff0fee`.
- استيراد نفس Backup مرتين: HTTP 200 في المرتين.
- نتيجة Regression بعد كل استيراد: 17/17 ونفس البصمة.
- فحص صياغة `server.js` وJavaScript داخل `index.html`: ناجح.

## ملاحظات النشر

- استخدم Node.js 20 وشغّل `npm ci` ثم `npm start`.
- يجب أن يكون `DATABASE_PATH` داخل Railway Volume دائم؛ وإلا ستضيع علامة إصدار السكيمة ويعود الترحيل الكامل مع كل حاوية جديدة.
- Cold Start الخاص بخطة Railway قد يضيف وقتًا خارج التطبيق، لكنه لم يعد يشغّل المصالحات الثقيلة مع كل فتح.
