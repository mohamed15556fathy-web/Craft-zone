# v10.6 — توحيد "السيولة الحالية" مع سجل الخزنة

## المشكلة اللي كانت موجودة

كارت **السيولة الحالية** كان بيقرأ من `cash_adjustments`، وكارت **الخزنة** بيقرأ من
`admin_cash_ledger`. أي حركة "تعديل السيولة الحالية" (تعيين / زيادة / نقصان رصيد) كانت
بتتسجل في الجدول الأول بس، فالخزنة مكانتش بتشوفها.

النتيجة في نسخة 8/9: السيولة **3,150 ج** والخزنة **-17,830 ج**، والفرق بينهم **20,980 ج**
بالظبط = حركة "تعيين الرصيد" اللي اتعملت يوم 2026-08-30 بعد الريسيت.

وكمان بند **"المتبقي بالخزنة/الشركة"** كان بيتحسب `السيولة − إجمالي كل العهد`، والخزنة
نفسها داخلة ضمن العهد، فكان بيطرح الخزنة من نفسها ويطلع رقم وهمي.

## اللي اتعمل

### 1. server.js — حركة مرآة للخزنة مع كل تعديل سيولة

- `syncCashAdjustmentCashLedger(id, createdBy)` — بتسجل/بتحدّث حركة في `admin_cash_ledger`
  للخزنة بنفس مبلغ التعديل، بـ `source_type='cash_adjustment'`.
  **مهم:** النوع ده مش `manual` عن قصد، لأن `getCashSummary` بتجمع الحركات اليدوية بس
  على السيولة — فمفيش أي احتمال يتحسب المبلغ مرتين.
- `syncAllCashAdjustmentCashLedgers()` — إعادة بناء شاملة، بتشتغل في:
  - الـ startup migration
  - `normalizeImportedBackupData` (بعد استرجاع أي باكب قديم)
  - بعد أي "تراجع" من سجل النشاط
- ربط الدوال دي في `POST /cash-adjustments` و `POST /cash-adjustments/:id` و
  `DELETE /cash-adjustments/:id` وفي حذف السجل من صفحة النشاط.
- `cash_adjustment` اتضاف لـ `IDEMPOTENT_CASH_SOURCE_TYPES` (مفيش تكرار عند إعادة المزامنة).
- `cleanupOrphanAutoFinancialLedgers` بتمسح أي حركة خزنة تعديلها الأصلي اتحذف.
- `cashAdjustmentSnapshotDefs` بقت تاخد نسخة من حركة الخزنة كمان، فالـ undo يرجّع الاتنين.
- وصف واضح للحركة في سجل الخزنة: "تعديل السيولة الحالية - تعيين رصيد - السبب".

### 2. accounts_admin_cash_patch.js — تصحيح البنود الثلاثة

- **إجمالي عهد الشغل (المستخدمين)** = عهد المستخدمين بس، من غير الخزنة.
- **المتبقي بالخزنة/الشركة** = رصيد الخزنة الفعلي من سجلها.
- بند أحمر بيظهر **بس** لو حصل فرق غير مفسر بين السيولة العامة و(الخزنة + العهد)،
  فأي اختلال جديد يبان فورًا بدل ما يفضل مستخفي.

المعادلة اللي المفروض تفضل صح دايمًا:

```
السيولة العامة = رصيد الخزنة + إجمالي عهد المستخدمين
```

## تصحيح الداتا في النسخة المرفقة

1. حذف الـ 5 أصول ثابتة (20,930 ج) وحركات خصمها من الخزنة.
2. حذف تعديل السيولة القديم (+20,980 ج) اللي كان بيعوّض خصم الأصول.
3. تسجيل تعديل جديد "تعيين الرصيد = 280,000 ج" (الأساس بعد التنظيف 3,100 ج، الفرق 276,900 ج)
   مع حركته المقابلة في سجل الخزنة.
4. إعادة بناء `balance_before` / `balance_after` لكل حركات السجل.
5. كل خطوة اتسجلت في سجل النشاط.

**الناتج:** السيولة الحالية = الخزنة = **280,000 ج**، والأصول الثابتة = 0.

## الاختبارات اللي اتعملت

اتشغّل السيرفر على النسخة المصححة واتجرب:

| العملية | السيولة | الخزنة | عهد المستخدمين |
|---|---|---|---|
| الحالة بعد التصحيح | 280,000 | 280,000 | 0 |
| إضافة تعديل +5,000 | 285,000 | 285,000 | 0 |
| تعديله لـ "تعيين 300,000" | 300,000 | 300,000 | 0 |
| حذف التعديل | 280,000 | 280,000 | 0 |
| شراء أصل ثابت 2,000 | 278,000 | 278,000 | 0 |
| حذف الأصل | 280,000 | 280,000 | 0 |
| تحويل عهدة 1,000 للخزنة → بودا | 280,000 | 279,000 | 1,000 |
| التحويل العكسي | 280,000 | 280,000 | 0 |

في كل الحالات: **السيولة = الخزنة + العهد**.
