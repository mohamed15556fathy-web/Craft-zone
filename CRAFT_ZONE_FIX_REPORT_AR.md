# تقرير إصلاح Craft Zone — المحاسبة والمخزون والمزامنة

التاريخ: 27 أغسطس 2026

## النتيجة

تم إصلاح المنطق العام بدون وضع أرقام الأوردرات المرجعية داخل كود التشغيل. أرقام الحالات المذكورة موجودة فقط داخل سكربت التحقق الآلي. تم حفظ نسخة المشروع وقاعدة البيانات الأصليتين قبل التعديل، ثم تشغيل الإصلاحات على نسخة معزولة.

## أهم الإصلاحات

- توحيد الحركات الآلية في `admin_cash_ledger` بمفتاح ثابت `source_type + source_ref`، مع تحديث الحركة الأصلية وحذف أي تكرار بدل إنشاء حركة جديدة.
- ربط الأصل الثابت بحركة `fixed_asset + asset_id` في الخزنة فقط؛ تعديل السعر يغير الأثر الصافي بالفرق، والحذف يزيل حركة الأصل المرتبطة ويعيد أثرها من رصيد الخزنة، ولا يدخل الأصل ضمن مصروفات التشغيل أو صافي الربح.
- فصل شراء الورق بالكيلو عن الشراء بالفرخ، وحفظ وحدة المستخدم والكمية والسعر الأصليين مع حساب الكيلو/الأفراخ والأسعار المكافئة في الحقول المنفصلة.
- ربط حركة شراء الورق القديمة الموجودة بالفعل بمفتاح `purchase + purchase_id` من غير إعادة إضافة المخزون.
- جعل قائد المجموعة فقط يحمل إجمالي البيع والمدفوع والمتبقي والشحن، وتصفير القيم المالية للأصناف الفرعية مع الإبقاء على تكلفة كل صنف.
- رفض أي تحصيل أو تغيير حالة عندما يكون المدفوع أكبر من إجمالي الأوردر، وتسجيل المشكلة في فحص الصحة بدل تعديل الأرقام صامتًا.
- إعادة حساب `sales_history` بعد تغييرات السعر والشحن والتأمين وExtra COD والتكاليف، مع احتساب خسارة الشحن والرسوم في المرتجعات.
- تحديث Snapshot ثابت في `cost_history` لكل حقل تكلفة حالي بدون إنشاء مصروف أو خصم خزنة جديد.
- منع تسليم قائد أوردر/مجموعة بسعر صفر، بدون إنشاء دفعة وهمية.
- المرتجع يحافظ على `total_price` و`paid_amount` ويجعل `remaining_amount = 0`، ويطلب قرارًا واضحًا: Refund فعلي أو الاحتفاظ بالعربون/الرسوم.
- Refund مسجل في `order_refunds` وحركة خزنة سالبة `refund + refund_id` مرة واحدة فقط.
- إعادة الشنط الجاهزة السادة الصالحة للبيع إلى المخزون مرة واحدة، مع إبقاء تكلفة الإنتاج خسارة للأوردر المصنع/المطبوع غير القابل للبيع.
- تخزين بيانات Bosta وقيمة المنتج على قائد المجموعة وقراءة قيمة الشحنة من إجمالي المجموعة.
- مزامنة المتجر تستخدم Upsert عند `ORDER_NOT_FOUND`، وثلاث محاولات Backoff للأخطاء المؤقتة، وقفلًا يمنع تزامن عمليتين لنفس المرجع، وتمسح `store_last_error` بعد النجاح.
- تعليق مزامنة المتجر أثناء Restore، وجعل Startup/Restore Reconciliation قابلًا للتكرار بدون تغيير الأرصدة.
- جعل تصنيف الفورمة `ربع/نصف/كامل` اختيارًا فنيًا صريحًا لا تستبدله الأبعاد. الأبعاد تعرض اقتراحًا فقط.
- إضافة تصنيف فني محفوظ للفورمة (`bag / box / pouch / other`) مشتق من نوع المنتج الصريح لا من المقاس، مع Snapshot داخل الأوردر.
- احترام تصنيف «ربع فرخ» الصريح في تكلفة السلوفان والتكسير والطباعة والزنكات. الترحيل يصحح فقط الصفوف المطابقة لبصمة الحساب القديم ولا يلمس تكلفة يدوية مختلفة.

## الملفات المعدلة وسبب التعديل

| الملف | السبب |
|---|---|
| `server.js` | منطق الخزنة والأصول والورق والمجموعات والدفعات والمرتجعات والمخزون وCost/Sales History وBosta وStore Sync وRestore وترحيلات قاعدة البيانات |
| `module_health.js` | فحوصات الأصول والورق والأوردرات والمخزون والتكلفة والمبيعات والحركات المالية والتصنيفات الصريحة، وربط الإصلاحات الآمنة |
| `orders_list.html` | واجهة قرار رد المبلغ أو الاحتفاظ به عند تحويل الأوردر/المجموعة إلى مرتجع |
| `forms.html` | حقول إلزامية للتصنيف الفني وتصنيف الفرخ، مع اقتراح منفصل لا يغيّر اختيار المستخدم |
| `lib/accounting_rules.js` | قواعد محاسبية نقية قابلة للاختبار: تحويل الورق، منع overpayment، المتبقي، Sales History، وتصنيف أخطاء المزامنة المؤقتة |
| `lib/form_rules.js` | قواعد تصنيف الفورمة ونوع المنتج والتحقق من الاختيارات الصريحة |
| `scripts/validate_regression.js` | فحص قاعدة البيانات والحالات المرجعية وجميع invariants المطلوبة مع بصمة محاسبية/مخزنية |
| `tests/accounting_rules.test.js` | اختبارات وحدة لقواعد الورق والدفعات والمرتجع والمبيعات والمزامنة |
| `tests/form_rules.test.js` | اختبارات وحدة لمنع استبدال تصنيف الفورمة ولمنع تخمين نوع المنتج من الأبعاد |
| `package.json` و`package-lock.json` | أوامر تشغيل الاختبارات وتثبيت نسخ الاعتماديات |

## نتائج الاختبارات

- فحص صياغة Node وملفات JavaScript داخل `forms.html` و`orders_list.html`: ناجح.
- اختبارات الوحدة: 10/10 ناجحة.
- Regression على نسخة قاعدة جديدة: 17/17 ناجحة.
- إعادة تشغيل نفس القاعدة وتشغيل Startup Reconciliation مرة ثانية: 17/17، والبصمة لم تتغير.
- استيراد نفس ملف Backup مرتين عبر `/import-backup`: الاستيرادان HTTP 200، وبعد كل استيراد 17/17، والبصمتان متطابقتان.
- اختبار المسارات الفعلية على قاعدة مؤقتة: إضافة/تعديل/حذف أصل، حفظ فورمة صريحة، مرتجع + Refund + رجوع مخزون، ومنع overpayment — 4/4 ناجحة، مع تكرار طلب المرتجع للتأكد من عدم التكرار.
- `PRAGMA integrity_check`: `ok`.
- `PRAGMA foreign_key_check`: لا توجد مخالفات.

البصمة المحاسبية/المخزنية بعد التشغيل والاسترجاع:

`c66aeb51f77abdc7e15f8fe1b3e04ff57f3caea9c2677fdd124f786f89ff0fee`

SHA-256 لقاعدة البيانات المسلمة:

`d63deb1d5490278f57dc573fa99c303407d87cdabb75e6952c3910649df6e475`

## الحالات المرجعية بعد الإصلاح

- شراء #12: `6000 فرخ`، وزن الفرخ `0.084 كجم`، الإجمالي `504 كجم`، سعر الفرخ `4.50`، الإجمالي `27000`، سعر الكيلو المحفوظ `53.571429`.
- إجمالي الأصول: `20930` جنيه، وكل أصل له حركة خزنة سالبة واحدة ولا يوجد مصروف تشغيل باسم أصل ثابت.
- `GRP-1782821361663`: `2250 / 2250 / 0`.
- `GRP-1782935425220`: `690 / 690 / 0`.
- `GRP-1785761735225`: الإجمالي `1270`، والمدفوع `0`، والمتبقي `1270` بدون دفعة وهمية.
- كل المرتجعات: المتبقي صفر مع الحفاظ على السعر والمدفوع التاريخيين.
- لا يوجد مخزون ورق أو يد أو شنط سالب.
- لا توجد دفعة أوردر أو دفعة مشتريات أو مصروف تشغيلي بلا Cash Ledger مقابل.
- لا توجد حركة آلية مكررة لنفس `source_type + source_ref`.
- آخر Cost Snapshot يطابق القيم الحالية، وSales History يطابق الأوردرات.
- الأوردر المعروض رقم 165: ثلاثة أصناف، ورق `9000` وسلوفان `4000` لكل صنف، وإجمالي التكلفة `51128.15` جنيه.

## التشغيل والتحقق

```bash
npm install
npm test
node scripts/validate_regression.js /path/to/database.db
npm start
```

قاعدة البيانات المسلمة Snapshot مستقل يضم محتوى WAL بالكامل، وليست نسخة خام ناقصة.

لم يتم إجراء اتصال حي بحساب Bosta أو موقع البيع لأن بيانات الاعتماد الخارجية لم تكن متاحة في بيئة الاختبار؛ تم اختبار قواعد Retry/Timeout برمجيًا، ومراجعة مسارات Upsert والقفل ومسح الخطأ محليًا.
