# تحديث v9.1 — أنواع المنتجات، ورق الزبدة، والإعدادات الكاملة

## 1) مكتبة الفورم
- **رجعت خانة «الفورمة»** في الجدول: ربع فرخ / نصف فرخ / فرخ كامل — وكمان في نافذة التفاصيل.
- في نموذج إضافة/تعديل فورمة فيه خانة بتوريك التصنيف وأنت بتكتب المقاس.
- التصنيف بيتحسب على **المقاس المقصوص فعلًا** (مقاس الفورمة + زيادة كسر السلاح).

## 2) مقاسات كل نوع منتج (إضافة أوردر + احسب أوردر + التطبيق)
| النوع | الخانات اللي بتظهر |
|---|---|
| شنط | الطول + العرض + المنفاخ |
| أكياس سندوتش | الطول + العرض + الجنب (اختياري) + **شكل الكعب** (بكعب / كعب لطش) |
| علب بطاطس · علب برجر | الطول + العرض + **الارتفاع** |
| كريب · باكيتات بطاطس | **مقاس جاهز**: صغير / وسط / كبير — وبيظهر بس المقاسات **الموجودة فعلًا في مكتبة الفورم** |
| أخرى | الطول + العرض + الارتفاع/العمق |

- اليد وخامتها ولونها والاستريشة ومخزن الشنط الجاهز بيظهروا **للشنط بس**.
- **باكيتات البطاطس** بقت مقاس جاهز في مكتبة الفورم كمان، مش طول وعرض وارتفاع.

## 3) الفورمة والمقاسات في الاتجاهين
- تكتب المقاس ← السيستم يرشّح الفورمة ويحطّ **⭐** على المطابقة تمامًا ويختارها.
- **تختار الفورمة من غير ما تكتب مقاس ← المقاسات بتتكتب لوحدها** (وده في كل أنواع المنتجات بما فيها الشنط).
- لو اخترت بإيدك، السيستم مابيبدّلش عليك.

## 4) ورق الزبدة — نوع ورق جديد
- **طباعة أوفست بس** — أي نوع طباعة تاني مرفوض.
- التكلفة = **ورق + قص + تصميم + طباعة أوفست**. مفيش تصنيع ولا يد ولا تركيب يد ولا استريشة.
- اتضاف في: إضافة أوردر · احسب أوردر · قائمة الأوردرات · مخزن الورق · المشتريات · مخطط القص · التطبيق.
- **سعر كيلو ورق الزبدة** في الإعدادات وتقدر تغيّره أي وقت.

## 5) صفحة الإعدادات — تبويب جديد «💵 الأسعار والتكاليف»
كل رقم كان مدفون في الكود بقى قابل للتعديل، ومقسّم على أقسام:

- **مقاسات القص والتشغيل** — زيادة كسر السلاح (1.5 سم) · حدود ربع/نص/فرخ (50×35 · 70×50 · 100×70)
- **أسعار الطباعة والزنكات** — الزنكة (60/150/250) · الطباعة (150/200/300) · التصميم (100) · سلك سكرين للقطعة (2) · زيادة المخزن الجاهز (5) · عدد القطع في التشغيلة
- **تكاليف التصنيع واليد** — القص (50) · تصنيع القطعة (1.2 كرافت · 1.5 و2 كوشيه + حد الـ30 سم) · تركيب اليد لكل 1000 (100) · أسعار أوامر تشغيل الشنط الجاهزة
- **تكاليف مراحل التشغيل** — التكسير والسلوفان والاستريشة وعدد الضربات في التشغيلة
- **أسعار الورق الافتراضية** — كرافت بني وأبيض · ورق طبع · كوشيه · برستول · **ورق الزبدة**

أي تعديل هنا بيسري على **السيرفر والسيستم والتطبيق** مع بعض — مفيش رقم متكرر في مكانين.

## 6) احسب أوردر — زرار «✂️ احسب بمقاس القص مباشرة»
منتج جديد لسه ملوش فورمة؟ دوس الزرار واكتب **إجمالي طول وعرض القص وعدد القطع من كل قصة** — والحساب بيشتغل على طول بالمقاس ده، وتظبط المقاسات والفورمة بعدين.

## ملفات اتعدّلت
**السيستم:** `server.js` · `forms.html` · `add_order.html` · `calculator.html` · `orders_list.html` · `orders_list_extracted.js` · `settings.html` · `inventory.html` · `inventory.js` · `purchases.html` · `cutting_optimizer.html`
**التطبيق:** `lib/models.dart` · `lib/add_order_screen.dart` · `lib/calculator_screen.dart` · `lib/order_tools.dart`
