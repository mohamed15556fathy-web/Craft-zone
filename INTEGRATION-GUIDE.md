# دليل ربط Craft Zone — السيستم + موقع البيع + الأبلكيشن

النسخة: الموقع `v11.2.0` • السيستم `v1.2.2` • الأبلكيشن `1.2.0+5`

---

## ١. الفكرة في سطرين

رقم الأوردر `CZ-YYMM-XXXX` بقى **رقم واحد موحّد** بين الموقع والسيستم.
السيستم هو اللي بيكلّم الموقع (مش العكس)، فمحتاج تفتح الموقع للنت بس، والسيستم يفضل مقفول.

```
موقع البيع  ←── يسحب أوردرات ودفعات ───  سيستم المصنع
            ───► يدفع حالات وأوردرات واتساب ──►
```

---

## ٢. التركيب (١٠ دقايق)

### أ) ولّد المفتاح المشترك

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### ب) على موقع البيع (Railway → Variables)

```
CZ_INTEGRATION_KEY=<المفتاح اللي ولّدته>
```

### ج) على سيستم المصنع (Railway → Variables)

```
STORE_BASE_URL=https://craft-zone-packing-production.up.railway.app
CZ_INTEGRATION_KEY=<نفس المفتاح بالظبط>
STORE_SYNC_ENABLED=1  # اختياري في النسخة الجديدة؛ وجود CZ_INTEGRATION_KEY يفعّل الربط تلقائيًا
STORE_SYNC_INTERVAL=25
```

> **النسخة الجديدة تقبل نفس اسم المتغير `CZ_INTEGRATION_KEY` في الطرفين**، وبتتجاوز مشكلة القيم القديمة الفارغة المحفوظة في قاعدة بيانات السيستم. `STORE_INTEGRATION_KEY` ما زال مقبول كاسم بديل.
>
> **أسهل طريقة**: من **قائمة الأوردرات** في السيستم، الشريط اللي فوق الجدول فيه زرار **⚙️ إعداد الربط** —
> تحط الرابط والمفتاح وتضغط حفظ، والربط يشتغل فورًا من غير إعادة تشغيل ولا دخول على صفحة الإعدادات.

### د) اتأكد إن الربط شغّال

افتح **قائمة الأوردرات** — الشريط هيقول `🔗 الربط شغّال`.
لو قال `⛔ الربط لسه مش متظبط` اضغط **⚙️ إعداد الربط** وكمّل البيانات.

### هـ) رقّم الأوردرات القديمة (مهم، مرة واحدة بس)

اضغط **🔢 ترقيم وترحيل الأوردرات القديمة** في نفس الشريط.

- كل أوردر قديم مالوش كود بياخد كود `CZ-YYMM-XXXX`.
- **الأوردر المجمّع بياخد كود واحد لكل أصنافه** (مش كود لكل صنف).
- الأوردرات اللي ليها كود بالفعل مبتتغيّرش.
- الأوردرات بتترحّل للموقع على دفعات علشان العميل يقدر يتابع — الباقي بيكمّل تلقائيًا كل دورة مزامنة.
- الترحيل التلقائي **بيعرض الحالة بس ومبيطلبش دفع**. لو عايز تطلب عربون من عميل معيّن، اضغط **🔗 لينك دفع العميل** على الأوردر بتاعه.

الكود بيظهر تحت اسم العميل في الجدول، وبالضغط عليه بيتنسخ. وتقدر تدوّر بيه في خانة البحث.

---

## ٣. سيناريو ١: عميل بيطلب من الموقع

1. يختار المقاس ويرفع اللوجو → المعاينة تظهر ومعاها تنبيه **«المعاينة دي تصميم مبدئي»**.
2. في صفحة الدفع:
   - **سادة** → يقدر يدفع عند الاستلام أو كامل.
   - **مطبوع** → الدفع عند الاستلام مخفي، ولازم **٣٠٪ عربون** أو المبلغ كامل، ولازم يعلّم ✅ على شروط التصميم.
3. يدفع بـ Paymob → الويب هوك يأكد → الحالة تبقى `تم الدفع`.
4. خلال ٢٥ ثانية الأوردر يدخل السيستم:
   - اسم البيدج: **Craft Zone Site**
   - الدفعة متسجلة **عربون** في `دفعات العميل` والخزنة
   - التكاليف التلقائية (قص/ورق/يد/طباعة) محسوبة زي أي أوردر عادي
   - الملاحظات فيها رقم الأوردر ولينك اللوجو

## ٤. سيناريو ٢: عميل بيطلب من واتساب

1. المودريتور يسجّل الأوردر عادي في السيستم.
2. من **⚙️ التحكم** يضغط **🔗 لينك دفع العميل**.
3. تظهر نافذة فيها الرقم الموحد + رسالة واتساب جاهزة + زرار **📲 إرسال واتساب للعميل**.
4. العميل يفتح اللينك → يكتب موبايله → يشوف المبلغ → يدفع.
5. الدفعة ترجع للسيستم أوتوماتيك كـ **عربون** على نفس الأوردر.

## ٥. سيناريو ٣: متابعة الحالة

أي تغيير حالة في السيستم بيوصل صفحة التتبع خلال ٢٥ ثانية:

| السيستم | اللي العميل بيشوفه |
|---|---|
| أوردر جديد | تم استلام الطلب |
| تحت الإنتاج / القص / الطباعة / التصنيع | الطلب قيد التصنيع |
| جاهز للشحن | جاهز للشحن |
| **تم الشحن** | **تم الشحن** (+ رقم تتبع بوسطة لو موجود) |
| تم التسليم | تم التسليم |
| مرتجع | مرتجع |

**الأوردر المجمّع**: بيظهر للعميل **أقل مرحلة** في الأصناف، يعني لو صنف اتشحن وصنف لسه بيتصنع → العميل يشوف «قيد التصنيع». ده مقصود علشان ما يحصلش لبس.

---

## ٦. سياسة العربون والتصميم

| الحالة | الدفع عند الاستلام | العربون |
|---|---|---|
| سادة / جاهز | ✅ متاح | اختياري |
| مطبوع | ❌ مقفول | **٣٠٪ على الأقل** أو المبلغ كامل |

لما العميل يختار **الدفع عند الاستلام**، اختيارات «عربون / المبلغ كامل» بتختفي خالص
وبيظهر بدالها: «هتدفع المبلغ كامل عند استلام الطلب، ومش مطلوب منك أي مبلغ دلوقتي».

النِسَب دي مطبقة **على السيرفر مش الواجهة بس** — يعني حتى لو حد حاول يبعت طلب مباشر هيترفض.

للتعديل: **لوحة تحكم الموقع → الإعدادات**
- `printedDepositPercent` = 30
- `designFee` = 100
- `printedCodEnabled` = false (خليها true لو حبيت تسمح بالاستلام للمطبوع)
- `designPolicyText` = نص الرسالة

**نص التصميم الافتراضي** بيظهر في ٤ أماكن (المعاينة، الدفع، صفحة النجاح، صفحة التتبع) وبيتخزن على الأوردر نفسه مع وقت الموافقة كإثبات:

> المعاينة اللي ظهرت لك تصميم مبدئي للشكل النهائي فقط. بعد دفع العربون هيتم إرسال التصميم الكامل خلال دقائق للمراجعة والاعتماد، ولو لم يتم الاتفاق يتم رد المبلغ كامل ويُخصم 100 ج حق تصميم فقط.

---

## ٧. حل المشاكل

| المشكلة | السبب والحل |
|---|---|
| الشريط بيقول «الربط متوقف» | في النسخة الجديدة: وجود `CZ_INTEGRATION_KEY` على السيستم يفعّل الربط تلقائيًا، ورابط الموقع له قيمة افتراضية. لو استمرت الرسالة سيظهر سبب الاتصال/المفتاح في نفس الشريط. |
| `INVALID_INTEGRATION_KEY` | المفتاحين مش متطابقين — راجع المسافات الزيادة |
| الأوردر مش داخل السيستم | لازم يكون مدفوع أو دفع عند الاستلام. الأوردرات اللي لسه `بانتظار الدفع` مبتترحّلش |
| `REF_CONFLICT` | الرقم مستخدم — السيستم بيولّد رقم جديد أوتوماتيك (٣ محاولات) |
| الدفع مش شغال | راجع متغيرات Paymob على الموقع (`PAYMOB_ENABLED=true` + المفاتيح) |
| عايز تشوف سجل المزامنة | جدول `store_sync_log` في قاعدة بيانات السيستم (آخر ٤٠٠ عملية) |
| «الربط مع الموقع غير مكتمل» عند الضغط على لينك الدفع | الرابط أو المفتاح ناقص — اضغط **⚙️ إعداد الربط** وكمّلهم |
| أوردر مالوش كود تتبع | اضغط **🔢 ترقيم وترحيل الأوردرات القديمة** |
| أوردر ليه كود بس مكتوب «لسه ما اترحّلش» | الربط متوقف أو الأوردر مفيهوش رقم موبايل — الترحيل بيحتاج رقم العميل |

**مهم**: لو الموقع وقع، السيستم بيفضل شغال عادي وبيكمّل المزامنة أول ما يرجع — مفيش أوردرات بتضيع، لأن الترحيل بيتأكد بـ ack من الطرفين.

---

## ٨. اللي اتعدل في كل مشروع

**موقع البيع**: `server.js` • `public/js/{account,checkout,customize,pay,admin-orders,admin-common}.js` • `public/{account,checkout,customize,success,payment-return,pay}.html` • `public/assets/{styles,tracking-pro,admin-gold}.css` • `data/store.json` • `.env.example`

**سيستم المصنع**: `server.js` • `orders_list.html` • `add_order.html` • `store_link_patch.js` (جديد) • `.env.example`

**الأبلكيشن**: `lib/{models,add_order_screen,order_details_screen}.dart` • `pubspec.yaml`


## v8.5 — Tracking + payment links
- كل أوردر في سيستم المصنع بياخد `store_ref` تلقائيًا ويتنشر لموقع البيع للتتبع حتى لو رقم الموبايل لسه فاضي.
- لينك الدفع يحتاج رقم موبايل على الأوردر لحماية الدفع، والعميل يقدر يختار من صفحة الدفع عربون أو سداد كامل.
- الأوردرات اللي فشل نشرها بتفضل في قائمة إعادة المحاولة بدل ما تتعلّم كمزامنة ناجحة.
