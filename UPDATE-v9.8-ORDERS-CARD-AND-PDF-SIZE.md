# تحديث v9.8 — كارت الأوردر على الموبايل ومقاس معاينة الـ PDF

## 1) معاينة الـ PDF بقت أصغر (الكمبيوتر والموبايل)

- ارتفاع صورة المعاينة اتغيّر من **126px على الكمبيوتر و148px على الموبايل** إلى **96px في الاتنين**.
- الكارت نفسه اتصغّر معاها: بادينج أقل، حواف أنعم، اسم الملف وزر التحميل بخط أصغر.
- ملفات الأوردر بقت في **شبكة عمودين** بدل ما تتراص تحت بعض:
  - عمود التحكم على الكمبيوتر اتوسّع من 190px لـ 220px علشان الملفين يقفوا جنب بعض.
  - أوردر بملفين كان بيخلي الصف **424px**، دلوقتي **251px**.
- إصلاح: الصورة كانت بتتقص جوّه المربع. دلوقتي الصفحة بتتعرض كاملة (`object-fit:contain` مع مربع بمقاس ثابت).
- الرسم بيتعمل بـ 1.3 ضعف المقاس المعروض، فالصورة تفضل واضحة على شاشات الريتينا رغم إنها صغيرة.

## 2) كارت الأوردر على الموبايل — كل البيانات ظاهرة ومتناسقة

- **مفيش طي**: زرار «باقي التفاصيل» اتشال، وكل خانات الأوردر بقت ظاهرة على طول، وبنفس ترتيب أعمدة الجدول.
- ترتيب الكارت: رقم الأوردر (هيدر) ← بيانات العميل ← بلاطات البيانات (عمودين) ← الدفع بعرض الكارت ← الحالة بعرض الكارت ← التحكم وملفات التصميم.
- كل خانة بقت **بلاطة** ليها نفس الحدود ونفس البادينج، والبلاطتين اللي في نفس السطر ليهم **نفس الارتفاع** بالظبط.
- أحجام موحّدة للأرقام (14px) والتفاصيل (10.5px) والتاجات، بدل ما كل خانة تيجي بمقاس من الـ inline style بتاعها.
- رقم الأوردر بقى بادچ فوق الكارت، وبيانات العميل في بلوك مميّز تحته.
- الحالة والقائمة المنسدلة بتاعتها بعرض الكارت كامل.
- تمييز بصري: أوردر مجمّع (أخضر)، أوردر من المخزن (سماوي)، وأصناف الأوردر المجمّع بشريط جانبي وإزاحة بسيطة.

## 3) إصلاحات فعلية اتكشفت أثناء الشغل

- **الأزرار ومعاينات الـPDF كانت بتخرج بره الكارت على الموبايل.**
  السبب: خانة التحكم كانت `flex-direction:column` مع `flex-wrap:wrap` وعناصر `flex:1 1 100%` —
  التركيبة دي بتحطّ كل عنصر في عمود لوحده بدل ما ينزلوا تحت بعض. اتحوّلت لشبكة عمود واحد.
- **صورة الـPDF كانت بتتضغط لـ 57px بدل ما تبان بمقاسها.**
  السبب: قاعدة أزرار الموبايل (`min-height:42px` + `flex:1 1 44%` + `font-size:13.5px`) كانت بتتطبق على
  زراير المعاينة والتحميل. اتعمل استثناء ليهم في صفحة الأوردرات بس.
- **خانة تحديد الطباعة** كانت ظاهرة في كل كارت على الموبايل حتى وإحنا مش في وضع الطباعة. دلوقتي بتظهر
  في وضع الطباعة بس، وبتيجي في أول الكارت.
- **عمود المصدر** كان `min-width:170px` فبيخلي عمود من الاتنين أعرض من التاني. اتظبط علشان البلاطات تبقى متساوية.

## 4) اللي ما اتغيّرش

- `mobile.js` و`server.js` **ما اتغيّروش ولا حرف**.
- أول 683 سطر من `mobile.css` (شل الموبايل، القائمة الجانبية، الجداول العامة، المودالات) **زي ما هي**؛
  كل التعديلات في قسم صفحة الأوردرات في آخر الملف، ومربوطة بـ `body.orders-page #ordersBody` علشان
  ما تلمسش أي صفحة تانية ولا جداول المودالات.
- الصلاحيات والحسابات والحالات والتوجيه والشحن والطباعة و Excel والفلاتر — كلها زي ما هي.
- باقي الصفحات اتغيّر فيها **رقم إصدار `mobile.css` و`mobile.js` بس** (من 9.7 لـ 9.8) علشان الموبايل
  يحمّل النسخة الجديدة فورًا وما يستخدمش الكاش القديم. نفس الحاجة اتعملت لـ `pdfjs`.

## ملاحظة للمستقبل (مش متغيّرة في التحديث ده)

المقاسات والتواريخ بتتعرض معكوسة أحيانًا (`25×15×8` بتبان `8×15×25`) لأن السطر عربي والأرقام بتترتب
حسب اتجاه الفقرة. ده موجود على الكمبيوتر والموبايل من قبل التحديث ده. لو تحب أظبطه، الحل إن نص المقاس
يتلفّ في عازل اتجاه — قوليلي وأعملها في تحديث صغير لوحده.
