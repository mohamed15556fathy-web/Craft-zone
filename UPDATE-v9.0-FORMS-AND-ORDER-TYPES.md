# تحديث v9.0 — تبسيط الفورم + ربط نوع المنتج بالأوردر

## 1) صفحة إدارة الفورم (forms.html)
- **اتشال عمود «المراجعة»** (تحليل الـAI ونسبة الثقة) من جدول مكتبة الفورم ومن نافذة التفاصيل.
- **اتشال عمود «التشغيل»** (ربع فرخ / نص فرخ / فرخ كامل) ومعاه بطاقتَي «تصنيف التشغيل التلقائي» و«إنتاج الفرخ 70×100».
- الجدول بقى 11 عمود بدل 13.

## 2) كيس السندوتش
- **شكل الكعب بقى خيارين بس**: `بكعب` أو `كعب لطش`.
  - الفورم القديمة بتتحوّل تلقائيًا مرة واحدة: `متكعب → بكعب` و`بدون كعب → كعب لطش` (في جدول `forms` وفي سناب‌شوت الأوردرات).
- **خانة الجنب بقت اختيارية بالكامل** ومستقلة عن شكل الكعب: تقدر تسيبها فاضية أو صفر مع أي خيار، والكيس ممكن يكون لطش أو متكعب بجنب أو من غيره.

## 3) صفحة إضافة أوردر + صفحة احسب أوردر (السيستم والتطبيق)
- **اتضافت خانة «نوع المنتج»** في صفحة احسب أوردر (كانت موجودة في إضافة أوردر بس).
- لما النوع يكون **أي حاجة غير «شنط»**:
  - بتختفي خانات **الطول والعرض والجنب** وبتظهر **الفورمة** مكانهم، والفورمة هي اللي بتحدد المقاس.
  - بتختفي **نوع اليد وخامة اليد ولون اليد**.
  - بتختفي **استريشة الأرضية**.
  - بيختفي **مخزن الشنط الجاهزة**.
- لما ترجع لنوع «شنط» بترجع الحقول واليد والاستريشة زي ما كانوا.

## 4) زيادة القص 1.5 سم
- أي شغل بيتقص على **مقاس فورمة** بياخد **+1.5 سم في الطول و+1.5 سم في العرض** تلقائيًا، عشان يبقى في فرق نكسر بيه السلاح.
- الزيادة دي داخلة في: مقاس القص، عدد القطع في الفرخ، تصنيف التشغيل (ربع/نص/فرخ)، وتكلفة الزنكة والطباعة — في السيرفر والسيستم والتطبيق مع بعض.
- **مقاس الفورمة المسجّل نفسه مابيتغيّرش** — الزيادة على المقاس المقصوص بس، وبتظهر في خانة «مقاس القص الفعلي».

## 5) ترشيح الفورمة تلقائيًا
- أول ما تكتب مقاس شنطة، السيستم بيدوّر على الفورمة المناسبة **ويختارها تلقائيًا**.
- القائمة مرتّبة بالأنسب، وعليها علامات:
  - **⭐** = مطابقة تمامًا في الطول والعرض والمنفاخ.
  - **◇** = قريبة من المقاس.
- تقدر دايمًا تغيّر الاختيار بإيدك، وساعتها السيستم مابيبدّلوش عليك.

## ملفات اتعدّلت
**السيستم:** `server.js` · `forms.html` · `add_order.html` · `calculator.html` · `orders_list.html` · `orders_list_extracted.js`
**التطبيق:** `lib/models.dart` · `lib/add_order_screen.dart` · `lib/calculator_screen.dart` · `lib/order_tools.dart`
