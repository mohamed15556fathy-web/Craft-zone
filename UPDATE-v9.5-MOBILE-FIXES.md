# تحديث v9.5 — إصلاح مشاكل الموبايل

بعد تجربة نسخة الموبايل على تليفون حقيقي ظهرت 3 مشاكل. دي أسبابها وحلولها.

---

## المشكلة 1: الصفحة مش بتتحرك فوق ولا تحت

### السبب
في `mobile.css` كان فيه السطر ده:

```css
html, body { overflow-x: hidden; }
```

الغرض منه كان منع التمرير الجانبي، بس **سفاري الآيفون عنده مشكلة معروفة**: أول ما يلاقي
`overflow` متحطوط على `html` أو `body`، بيحوّل الصفحة لحاوية تمرير جوّه حاوية تمرير،
والنتيجة إن التمرير الرأسي بيقف تماماً — خصوصاً لما يبقى في الصفحة عناصر `position:fixed`
(زي الشريط العلوي والسفلي).

وكان فيه سبب تاني مساعد: القائمة الجانبية كانت بتحط `overflow:hidden` على `html` وهي مفتوحة
علشان تمنع التمرير وراها، ولو الكلاس ده فضل عالق (مثلاً برجوع الصفحة من كاش المتصفح)
الصفحة بتتقفل خالص.

### الحل
1. **اتشال أي `overflow` من `html` و `body` نهائياً.** بدل ما نمنع التمرير الجانبي بالقوة،
   اتظبطت العناصر نفسها ما تزيدش عن عرض الشاشة — واتأكدنا بالاختبار إن مفيش ولا صفحة
   فيها تمرير جانبي (0 بكسل زيادة في الـ 24 صفحة).
2. **اتشال قفل التمرير العام بتاع القائمة الجانبية** واتحل محله حل موضعي:
   `touch-action:none` على خلفية القائمة و `overscroll-behavior:contain` على قائمة اللينكات.
3. زرار القائمة (☰) بقى **يفتح ويقفل** بدل ما يفتح بس.
4. لما الصفحة ترجع من كاش المتصفح (Back) بتتأكد إن القائمة مقفولة.

### تحسينات سرعة كمان (نفس العرض بالظبط)
- اتشال `backdrop-filter: blur()` من الشريط العلوي والسفلي — الفلتر ده بيتحسب كل فريم
  أثناء التمرير وبيعمل تقطيع على التليفون. الخلفية بقت لون صريح، والشكل زي ما هو.
- اتشالت ظلال الكروت الثقيلة، والظلال الباقية بقت أخف.
- اتشالت عملية `cloneNode` من كود تصنيف الخلايا — كانت بتتنفذ لكل خلية أزرار في الجدول
  (يعني مئات المرات في صفحة الأوردرات).

---

## المشكلة 2: الأوردرات مش بتتعرض صح / صفوف مكررة

### السبب
في وضع الكروت كان مكتوب:

```css
table.cz-cards tr { display: block !important; }
table.cz-cards td { display: flex  !important; }
```

الـ `!important` دي كانت **بتلغي أي إخفاء للصفوف بيعمله السيستم نفسه**. وصفحة الأوردرات
بتعتمد على ده فعلاً: أصناف الأوردر المجمّع بتتخزن كصفوف مخفية بـ
`style="display:none"` وبتظهر لما تضغط "عرض الأصناف". مع الـ `!important` الصفوف دي
كانت بتفضل ظاهرة على طول، فالقائمة بتبان مكررة وغير مرتبة.

### الحل
- اتشالت `!important` من كل خصائص `display` في وضع الكروت (الـ specificity كفاية من غيرها).
- اتضافت قاعدة صريحة إن أي صف أو خلية عليها كلاس `hidden` أو خاصية `hidden` تفضل مخفية.

### الاختبار
| الحالة | النتيجة |
|---|---|
| `row.style.display='none'` على صف | مخفي ✓ |
| رجوع الصف بعد `display=''` | ظاهر ✓ |
| كلاس `hidden` على صف | مخفي ✓ |
| `td.style.display='none'` على خلية | مخفية ✓ |

---

## المشكلة 3: الأوردرات مش مترتبة (كارت طوله شاشة كاملة)

### السبب
جدول الأوردرات فيه **15 عمود**. أول نسخة كانت بتحول كل صف لكارت فيه 15 سطر تحت بعض،
يعني الكارت الواحد طوله كان **1300 بكسل** والصفحة كلها بـ 26 أوردر طولها **33,673 بكسل**.
يعني الأوردر الواحد أطول من شاشة التليفون — مستحيل تتصفح القائمة كده.

### الحل: كارت مضغوط
- الكارت بقى **شبكة من عمودين** بدل سطر لكل عمود.
- **عنوان الكارت بقى اسم العميل** (كان رقم الأوردر) — أوضح بكتير وانت بتدوّر.
- **ملخص ظاهر** بأهم 6 خانات: م، التاريخ، الكمية، البيع، الدفع، الحالة.
- **باقي التفاصيل** (المصدر، المقاس، الطباعة، سعر الشحن، التكلفة، التصميم) بتتفتح
  بلمسة على الكارت — فيه زرار "⌄ باقي التفاصيل" تحت.
- الخانات الطويلة بتتقص وبيبان عليها تدرّج بسيط، وبتظهر كاملة أول ما تفتح التفاصيل.
- خانة تحديد الطباعة (التشيك بوكس) راحت مع التفاصيل بدل ما تاخد سطر كامل.

### النتيجة بالأرقام
| | قبل | بعد |
|---|---|---|
| طول الكارت الواحد | 1,300 px | **457 px** |
| طول صفحة 26 أوردر | 33,673 px | **12,212 px** |

الضغط ده اتطبق أوتوماتيك على **أي جدول** أعمدته أكتر من 6 (الأوردرات، المديونيات...)،
والجداول الأصغر (المخزن، العملاء، اليد) فضلت بشكلها الأول لأنه كان مريح أصلاً.

> لمسة على أي مكان فاضي في الكارت = فتح/قفل التفاصيل.
> الضغط على زرار أو قائمة جوه الكارت **مش** بيفتح التفاصيل — اتأكدنا من ده بالاختبار.

---

## الاختبارات

- **24 صفحة** على مقاس iPhone 13: التمرير شغال في كلها، مفيش تمرير جانبي (0 بكسل)،
  مفيش `overflow` متحطوط على `html` أو `body` في أي صفحة، ومفيش أي خطأ JavaScript.
- **اختبار وظيفي**: إضافة عميل من الموبايل → اتحفظ والكارت ظهر. فتح مودال كشف الحساب →
  اشتغل وبيغطي الشريطين. القائمة الجانبية بتفتح وتقفل وتنقّل صح.
- **الديسكتوب**: مقارنة صور بالبكسل بين النسخة الأصلية والجديدة على 1440×900 لكل الصفحات
  الرئيسية → **مطابقة 100%** (0 بكسل مختلف).

---

## ملخص الملفات

| الملف | التغيير |
|---|---|
| `mobile.css` | إصلاح التمرير + الكارت المضغوط + تحسينات سرعة |
| `mobile.js` | إصلاح تصنيف الخلايا + الملخص/التفاصيل + قفل القائمة |
| صفحات HTML | **ما اتغيرتش** في التحديث ده |
| `server.js` | **ما اتغيرش** خالص |
