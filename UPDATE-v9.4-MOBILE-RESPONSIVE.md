# تحديث v9.4 — نسخة الموبايل (Mobile Responsive)

الهدف: اللي يفتح السيستم من الكمبيوتر يشوفه **زي ما هو بالظبط**، واللي يفتحه من الموبايل
يشوف واجهة متناسقة شكلها تطبيق موبايل حقيقي.

---

## 1) الملفات اللي اتضافت

| الملف | الوظيفة |
|---|---|
| `mobile.css` | كل تنسيقات الموبايل. **كل** قواعده مكتوبة جوّه `@media (max-width:768px)` — يعني أي شاشة أعرض من 768px مش بتتأثر بأي حرف فيه. |
| `mobile.js` | واجهة التطبيق على الموبايل: شريط علوي + شريط سفلي + قائمة جانبية + تحويل الجداول لكروت. أول سطر فيه بيتأكد إن الشاشة موبايل، ولو الشاشة كمبيوتر بيقف فوراً ومش بيعمل أي حاجة. |

## 2) التعديل على صفحات HTML (24 صفحة)

كل صفحة اتضاف لها 3 سطور بس قبل `</head>` — مفيش أي تغيير تاني في أي صفحة:

```html
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<link rel="stylesheet" href="mobile.css">
<script src="mobile.js"></script>
```

> 13 صفحة كانت أصلاً **ناقصة** الـ viewport meta (منها `index.html` و `orders_list.html`
> و `accounts.html` و `add_order.html` و `inventory.html` و `settings.html`)، وده السبب
> الرئيسي إن الموبايل كان بيعرضها بشكل مصغّر وغير متناسق. دلوقتي كل الصفحات فيها.

**`server.js` ما اتغيرش خالص.** الملفات الجديدة بتتقدّم من نفس مسار السيرفر الحالي
(`.css` و `.js` مسموح بيهم أصلاً في `SAFE_ROOT_FILES`).

---

## 3) اللي بيحصل على الموبايل

### شكل التطبيق
- **شريط علوي ثابت** فيه اسم الصفحة + اسم المستخدم ودوره + زرار القائمة (☰) + زرار رجوع.
- **شريط سفلي (Bottom Tabs)** فيه: الرئيسية / الأوردرات / زرار ➕ بارز في النص / الورق / الحسابات
  — والأزرار دي **بتحترم صلاحيات المستخدم**: اللي مالوش صلاحية صفحة مش بيشوفها، وبيتحط
  مكانها أول صفحة متاحة ليه.
- **قائمة جانبية** فيها كل صفحات السيستم (مفلترة بالصلاحيات كمان) + بيانات المستخدم + تسجيل خروج.

### المحتوى
- **الجداول بتتحول كروت**: كل صف بيبقى كارت فيه اسم العمود جنب القيمة، والأزرار بتاعت الصف
  بتبقى بعرض الكارت — بدل التمرير اليمين والشمال. الجداول المعقدة اللي مش بتنفع كروت
  بتفضل جدول بيتزحلق أفقياً بالإصبع.
- **الفورمات** بقت عمود واحد، وكل خانة إدخال ارتفاعها مريح للمس وحجم خطها 16px
  (ده بيمنع الآيفون إنه يكبّر الشاشة لوحده أول ما تدوس على أي خانة).
- **المودالات** بقت شاشة كاملة زي الـ Sheets في التطبيقات، وبتغطي الشريط العلوي والسفلي.
- **قائمة الرئيسية** بقت شبكة أيقونات عمودين زي شاشة تطبيقات.
- **كروت الأرقام** عمودين، و**أزرار الهيدر** اتظبطت في صفّين مرتبين.
- مفيش أي تمرير أفقي في أي صفحة، وفيه دعم لـ safe area بتاع الآيفون (النوتش والشريط السفلي).

---

## 4) الاختبارات اللي اتعملت

- **24 صفحة** اتفتحت على مقاس iPhone 13 (390×844): مفيش ولا صفحة فيها تمرير أفقي،
  ومفيش أي خطأ JavaScript.
- **الجداول**: 52 جدول في 18 صفحة اتحولوا كروت بنجاح، وكل الخلايا بتاخد عناوين أعمدتها صح
  — بما فيها الصفوف اللي بتتولّد بعدين من الـ JS (فيه MutationObserver شغال).
- **اختبار وظيفي**: إضافة عميل جديد من واجهة الموبايل → اتحفظ، والعدّاد زاد، والكارت الجديد ظهر
  متنسق. فتح مودال كشف الحساب من كارت → اشتغل. القائمة الجانبية بتفتح وبتنقّل صح.
- **الديسكتوب**: مقارنة صور بالبكسل بين النسخة القديمة والجديدة على مقاس 1440×900
  (صفحة كاملة بالتمرير) لـ 12 صفحة → **مطابقة 100%** (فرق أقل من 3/255 في 20 بكسل بس،
  وده مجرد تنعيم حروف عشوائي مش فرق تصميم).

---

## 5) لو حبيت تظبط حاجة بعدين

- **تغيير حد الموبايل** (768px): غيّر رقم `max-width: 768px` في أول `@media` في `mobile.css`.
- **تغيير أزرار الشريط السفلي**: عدّل مصفوفة `TAB_ORDER` في أول `mobile.js`.
- **منع جدول معيّن من إنه يتحوّل كروت**: ضيف `data-cz="off"` على تاج الـ `<table>`.
- **إخفاء أي عنصر من الموبايل بس**: ضيف عليه كلاس `no-mobile`.
- **إلغاء الموضوع كله**: امسح السطور التلاتة من الـ `<head>` — السيستم يرجع زي ما كان بالظبط.
