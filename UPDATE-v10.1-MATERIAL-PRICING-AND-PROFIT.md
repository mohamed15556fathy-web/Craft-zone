# تحديث v10.1 — تسعير وأرباح الخامات + صندوق تدوير الورق

الهدف: تبيع الورق واليد بربح، تعرف كسبت منهم كام بالظبط، وتضمن إن فلوس الورق ترجع ورق تاني.

---

## 1) سعر بيع لكل خامة (زي مخزن الشنط بالظبط)

| الجدول | اتضاف | المعنى |
|---|---|---|
| `paper` | `sell_price_kg`, `sell_price_sheet`, `margin_percent`, `price_mode` | سعر بيع الورق |
| `handles` | `sell_price`, `margin_percent`, `price_mode` | سعر بيع اليد |

`price_mode` بياخد قيمتين:
- `type` (الافتراضي) → الصنف بيورث سعر نوعه أوتوماتيك
- `custom` → الصنف له سعر خاص بيكسر سعر النوع

### ترتيب تحديد السعر (من الأقوى للأضعف)
```
سعر خاص بالصنف
  ← سعر النوع (نوع + لون + جرام)
  ← سعر النوع (نوع + لون)
  ← سعر النوع (نوع + جرام)
  ← سعر النوع (النوع بس)
  ← هامش % فوق سعر الشراء
  ← سعر الشراء (fallback آمن)
```

يعني تقدر تقول **"البرستول كله بـ ٧٠"** مرة واحدة، وتستثني صنف واحد بسعر مختلف لو حبيت.

**جدول `material_price_settings`** بيخزن أسعار الأنواع. بيتزرع تلقائيًا بصفوف فاضية لكل نوع (كرافت بني/أبيض/أسود، ورق طبع، برستول كوشيه، كوشيه، ورق زبدة، مجدول، دوباره، ستان).

---

## 2) 🔴 إصلاح مشكلة تغيّر الأرباح بأثر رجعي

**المشكلة قبل التحديث:** `paper_history` كان بيخزن الكميات بس. تكلفة أي أوردر قديم كانت بتتحسب دايمًا بـ `buy_price_kg` **الحالي**. يعني أول ما تشتري ورق بسعر أغلى، أرباح كل الأوردرات القديمة بتتغيّر لوحدها.

**الحل:** أعمدة سناب شوت على كل حركة مخزن:

| العمود | المعنى |
|---|---|
| `unit_cost` | سعر شراء الوحدة لحظة الحركة |
| `unit_sell` | سعر بيع الوحدة لحظة الحركة |
| `cost_value` | إجمالي التكلفة |
| `sell_value` | إجمالي المبيع |

اتضافت في `paper_history` و `handles_history`.

**الحركة بقت ثابتة للأبد.** تغيّر السعر النهاردة مش هيلمس ولا أوردر قديم.

### ليه استخدمنا SQLite Triggers؟
السيستم فيه **١٨ نقطة مختلفة** بتكتب في `paper_history` (قص، أوامر تشغيل، مشتريات، تعديل أوردر، حذف أوردر، مخطط القص الذكي…). بدل ما نعدّل ١٨ مكان ونسيب ثغرات، حطينا تريجرات على مستوى قاعدة البيانات:

- `trg_paper_history_pricing` / `trg_handles_history_pricing` → بيملّوا السناب شوت
- `trg_paper_history_replenish` / `trg_handles_history_replenish` → بيدخّلوا الحركة صندوق التدوير

**أي حركة من أي مكان — حتى الأكواد اللي هتتكتب بعدين — بتتسجل صح تلقائيًا.**

---

## 3) 🔒 صندوق تدوير الخامات

> "فلوس الورق مدخلهاش في شغل تاني"

**جدول `material_replenishment_ledger`:**
- كل **خصم** من المخزن → `reserve` بمبلغ = الكمية × سعر الشراء **الحالي** (سعر الإحلال، مش السعر القديم، عشان الغلا ميأكلش المخزن)
- كل **توريد** → `restock` بيخصم من الصندوق

**المعادلة:** `المستحق = المحجوز − المورّد`

الكارت في صفحة الورق بيقولك:
- محجوز لإعادة الشراء
- اتشرى فعلاً
- ⚠️ ناقص تشتري بـ **← الفلوس دي متتصرفش**
- محتاج تجيب إيه بالظبط — **حسب النوع** و **صنف صنف** (بالكيلو والفرخ والقيمة)

**التصفير:** `material_replenishment_settlements` بيقفل الدورة الحالية ويبدأ عدّ جديد **من غير ما يمسح أي سجل**. الدورات القديمة بتفضل ظاهرة.

> ⚠️ **أول مرة بس:** رصيد المخزون الافتتاحي بيتحسب كتوريد. اضغط تصفير مرة واحدة بعد ما تدخل أرصدتك.

---

## 4) التسعيرة بسعر البيع + فصل ربح الخامات

**قاعدة أساسية اتحافظ عليها:** `cost_paper` و `cost_hand` على الأوردر **لسه بسعر الشراء الحقيقي** — لأن `module_health.js` والحسابات بيعتمدوا عليهم. لو غيّرناهم كان السيستم كله هيولّع تحذيرات وهيحسب ربح مضاعف.

بدل كده، **ربح الخامات بيتحسب مشتقًا** من سجل المخزن:

```
ربح الخامات = Σ(sell_value) − Σ(cost_value)   لحركات الأوردر
```

### في الحاسبة (`calculator.html`)
- قايمة **"تسعير الخامات"**: بسعر البيع (افتراضي) / بسعر الشراء
- **"هامش الخامات الافتراضي %"** للأصناف اللي لسه ملهاش سعر بيع
- الفرخ الافتراضي بقى ليه خانة **"سعر بيع الكيلو"**
- كروت جديدة في النتيجة: تكلفة الخامات الحقيقية | **ربح الخامات** | **ربح التشغيل** | إجمالي الربح المتوقع
- تحذير أحمر لو سعر البيع أقل من الشراء

### في تقارير الأرباح
`estimateExpectedProfitForOrder` بقى بيرجّع كمان:
`material_margin` • `material_cost` • `material_charge` • `conversion_profit`

---

## 5) الشاشات الجديدة

`material_pricing_patch.js` — ملف مستقل بيشتغل فوق `inventory.html` و `handles.html` من غير ما يلمس أي كود قديم. ٤ تبويبات:

**📊 الأرباح** (مع فلتر تاريخ)
استهلكت كام • تكلفتها الحقيقية • بعتها بكام • **ربح الخامات** • اشتريت كام • الفرق (استهلاك − شراء) • قيمة المخزون بالشراء وبالبيع • الربح المخزّن في الرف • تفصيل حسب النوع • أعلى الأصناف ربحًا

**🔒 صندوق التدوير** — الشرح فوق

**🏷️ أسعار البيع** — تحديد سعر النوع + السعر الفعلي لكل صنف مع إمكانية تخصيصه أو رجوعه للنوع

**📜 سجل الأسعار** — مين غيّر إيه من كام لكام وامتى، بالنسبة المئوية

**بالإضافة:**
- عمودين جداد في جدول الورق: `سعر البيع/كجم` و `الربح/كجم`
- عمودين جداد في جدول اليد: `سعر البيع` و `الربح/قطعة`
- خانة سعر بيع في فورم الإضافة/التعديل في الصفحتين
- **حارس الخسارة:** شريط أحمر أول ما صنف يبقى سعر بيعه ≤ سعر شرائه

---

## 6) الـ Endpoints الجديدة

| Endpoint | الوظيفة |
|---|---|
| `GET /material-prices` | قائمة الأسعار + السعر الفعلي لكل صنف + التحذيرات |
| `POST /material-price-rule` | حفظ سعر على مستوى نوع |
| `POST /material-item-price` | سعر خاص بصنف أو رجوعه للنوع |
| `GET /material-price-history` | سجل تغيّر الأسعار |
| `GET /material-profit-summary` | كروت الأرباح (بفلتر تاريخ) |
| `GET /material-replenishment` | صندوق التدوير وتفاصيله |
| `POST /material-replenishment/settle` | تصفير الصندوق |
| `GET /orders-material-margin` | ربح الخامات لكل أوردر |

**الصلاحيات:** القراءة تحتاج `perm_view_inventory` أو `perm_view_handles` — الكتابة تحتاج `perm_manage_paper` أو `perm_manage_handles`.

---

## 7) الملفات المتغيّرة

```
server.js                       أعمدة + ٤ جداول + ٤ تريجرات + محرك التسعير + ٨ endpoints
inventory.html                  عمودين + خانة سعر بيع + ربط الباتش
handles.html                    عمودين + خانة سعر بيع + ربط الباتش
calculator.html                 تسعير بسعر البيع + كروت تقسيم الربح
material_pricing_patch.js       جديد — كل شاشات التسعير والأرباح
```

---

## 8) الاختبارات اللي عدّت ✅

- إنشاء الجداول والتريجرات على قاعدة نضيفة
- توارث سعر النوع على الأصناف
- التسعير بالهامش % لليد
- **الحركة القديمة فضلت بسعرها القديم بعد رفع السعر** (١٦٥٠ مش ١٨٠٠) ← الاختبار الأهم
- صندوق التدوير: حجز وخصم وتصفير
- سجل تغيّر الأسعار
- حارس الخسارة
- `npm test` → ١٠/١٠
- `/system-health-check` و `/professional-health` → مفيش مشاكل جديدة

---

## المرحلة الجاية (مقترحة)

1. **أيام التغطية** بدل الحد الأدنى الجامد — "الورق ده يكفي ١٢ يوم"
2. **قائمة شراء مقترحة** بضغطة واحدة تبقى أمر شراء
3. **نسبة الهالك (العادم)** — الفرق بين الورق المفروض والمستهلك فعلاً
4. **حارس التسعيرة** — تحذير قبل حفظ أوردر مسعّر تحت تكلفة خاماته
5. **مخزن البواقي** — الفرش المقصوصة ليها قيمة وبتترمي دلوقتي
6. **مقارنة الموردين** — اتجاه سعر الشراء لكل مورد بالوقت
7. **متوسط التكلفة المرجّح (WAC)** بدل آخر سعر شراء

---

# تحديث v10.2 — إصلاحات وتبسيط

## 🔴 إصلاح: سعر النوع مكانش بيتطبق

**السبب:** السيستم بيزرع صفوف أسعار فاضية لكل نوع (كرافت+بني، كرافت+أبيض…) بسعر صفر.
لما كنت تحدد سعر لـ "كرافت / كل الألوان"، الصف الفاضي **الأخص** (كرافت+بني) كان بياخد
الأولوية في الترتيب ويرجّع صفر، فالسعر العام مكانش بيوصل للأصناف.

**الحل:** أي قاعدة من غير سعر ولا هامش بتتشال من الترتيب أصلاً قبل المقارنة.

اتأكد بالاختبار: سعر عام ← يتطبق، سعر أخص ← يكسر العام، تغيير السعر ← يتطبق فورًا.

## 🎨 اللون بقى قايمة اختيارات

في تبويب أسعار البيع، خانة اللون بقت `select` بنفس اختيارات صفحة الأصناف
(بني / أبيض / أسود + أي لون موجود فعلاً في مخزنك) + خيار **"كل الألوان"**.
والجرام كمان بقى قايمة من الجرامات الموجودة عندك.
والأنواع اللي لونها ثابت أبيض (ورق طبع / كوشيه / برستول كوشيه / ورق زبدة) اللون فيها
بيتقفل على أبيض تلقائي — بالظبط زي فورم الأصناف.

**كمان:**
- حقل "الهامش %" بيختفي لما تختار "سعر ثابت"، والعكس — عشان متحتارش
- سطر تحت الفورم بيقولك **"هيتطبق على ٣ أصناف: كرافت • بني"** قبل ما تحفظ

## 📉 "الورق الناقص" بدل "صندوق التدوير"

نفس الفكرة بس بلغة واضحة وأرقام بالسالب زي ما طلبت:

```
⚠️ الورق خرج ولسه مرجعش      −١٬٢٠٠ ج
📉 خرج من المخزن              −٣٠ كجم
📈 دخل المخزن                 +٠ كجم
```

- كل ورق يخرج (قص/خصم) → الرقم ينزل بالسالب
- كل ورق يدخل (فاتورة شراء أو إضافة يدوية) → الرقم يرجع لفوق **لوحده**
- يوصل صفر = كل اللي خرج رجع
- زرار **"الرقم ده جاي منين؟"** بيفتح كل الحركة حركة

## 📖 شرح داخل كل شاشة

كل تبويب بقى فوقه صندوق أزرق بيشرح بالعامية إيه اللي بيتحسب، بمثال بالأرقام:
> استهلكت ١٠٠ كجم شاريهم بـ ٤٠ (٤٬٠٠٠ ج) وبايعهم بـ ٥٥ (٥٬٥٠٠ ج) ← كسبت ١٬٥٠٠ ج

وترتيب التبويبات اتغير: **أسعار البيع** بقت أول حاجة تفتح.

## 🧹 إخفاء الأصناف الخالصة

الصنف اللي رصيده صفر بيتخفي تلقائي من مخزن الورق ومخزن اليد.
تحت الجدول بيقولك **"متخفي ٤ أصناف خالصة"**، وفيه مربع اختيار **"إظهار الأصناف الخالصة"**
لو حبيت ترجّعها (اختيارك بيتحفظ). الأصناف مش بتتمسح — بتتخفي بس، وأول ما توردها تاني بترجع لوحدها.

## 🗂️ صفحة الورشة

كل الأقسام بقت **مطوية افتراضيًا** ما عدا **"📊 الورشة كسبت ولا خسرت؟"**.
الكروت العلوية اتلمّت في قسم **"📈 ملخص الفترة"** قابل للطي، وقسم الفترة كمان
(أزرار الأسبوع/الشهر بتفضل ظاهرة في عنوانه حتى وهو مطوي).
واتضاف زرارين في الهيدر: **⬇️ فتح الكل** و **⬆️ طي الكل**. اختيارك بيتحفظ لكل قسم.

---

# تحديث v10.3 — 🔴 إصلاح السبب الحقيقي

## المشكلة: `no such table: material_price_settings`

كل التعديلات كانت مكتوبة صح، بس **السيستم ماكانش بينفّذها أصلاً على قاعدتك**.

السبب في `server.js`:

```js
const DATABASE_SCHEMA_VERSION = 'craft-zone-v10.0-workshop-costs-production-20260828';

async function ensureSchema({ force = false } = {}) {
  if (!forceMigration && await hasCurrentDatabaseSchemaVersion()) {
    return { mode: 'fast', ... };   // ← بيخرج من غير ما ينفّذ أي CREATE TABLE
  }
  ...
}
```

السيستم بيسجّل رقم إصدار السكيمة جوه قاعدة البيانات. لو الرقم موجود، بيعمل **Fast Start**
ويتخطّى كل أوامر إنشاء الجداول والأعمدة (ده مقصود، عشان السيرفر يقوم بسرعة).

قاعدتك كان عليها علامة `v10.0` من قبل، فأول ما رفعت الكود الجديد
السيرفر قال "أنا مترحّل خلاص" وتخطّى **كل** الجداول والأعمدة الجديدة.

**علامة مميزة للمشكلة دي:** عمود سعر البيع كان بيظهر `— لسه` وعمود الربح
بيظهر بالسالب (`−57.6 ج`) — لأن `sell_price_kg` مكانش موجود أصلاً فبيتقرا صفر،
و `0 − 57.6 = −57.6`.

## الحل — طبقتين

**١) رفع رقم إصدار السكيمة**
```js
const DATABASE_SCHEMA_VERSION = 'craft-zone-v10.2-material-pricing-20260906';
```
أول تشغيل بعد الرفع هيشغّل الترحيل الكامل مرة واحدة وينشئ كل حاجة، وبعدها
يرجع لمسار Fast Start عادي.

**٢) إصلاح ذاتي كسول (شبكة أمان)**
كل سكيمة التسعير اتلمّت في دالة واحدة `ensureMaterialPricingSchema()`،
وبتتنده من **كل** endpoint بيلمس الجداول دي + من `/get-paper` و `/get-handles`.
يعني حتى لو الترحيل اتخطّى لأي سبب، أول ما تفتح صفحة الورق الجداول بتتعمل لوحدها.
النتيجة بتتخزن في متغير، فمفيش أي حِمل على الطلبات اللي بعدها.

## الاختبار

بنيت قاعدة بيانات **مطابقة لحالة الإنتاج عندك**: عليها علامة `v10.0`
ومفيهاش أي جدول تسعير. شغّلت السيرفر الجديد عليها:

```
العلامة بعد التشغيل: v10.0 | v10.2  ✅
اتعمل: material_price_settings, material_price_history,
       material_replenishment_ledger, material_replenishment_settlements,
       trg_paper_history_pricing, trg_paper_history_replenish,
       trg_handles_history_pricing, trg_handles_history_replenish  ✅
أعمدة الورق: sell_price_kg, sell_price_sheet, price_mode  ✅

كرافت كله بـ 65      → اتحدّث 2 صنف ✅
برستول كوشيه بـ 68   → اتحدّث 1 صنف ✅

  أبيض 250جم برستول كوشيه: شراء 51.43 → بيع 68 | ربح 16.57
  أبيض 100جم كرافت:        شراء 60    → بيع 65 | ربح 5
  بني 240جم كرافت:         شراء 51    → بيع 65 | ربح 14

خصمت 40 كجم → −2057.2 ج   ✅
رجّعت 40 كجم →      0 ج   ✅
```

## ملاحظة عن اللون

الصورة اللي بعتها كانت من نسخة **v10.1**. قايمة اختيارات اللون اتعملت في **v10.2**
واللي كنت لسه مرفوعهاش. النسخة دي فيها:
- اللون = قايمة منسدلة (كل الألوان / بني / أبيض / أسود + أي لون في مخزنك)
- الجرام = قايمة منسدلة بالجرامات الموجودة عندك
- الأنواع بيضاء اللون (كوشيه، برستول، ورق طبع، زبدة) بيتقفل لونها على أبيض تلقائي
- رابط الملف بقى `?v=10.2` عشان المتصفح ياخد النسخة الجديدة مش المحفوظة في الكاش

---

# تحديث v10.4 — ليه الربح كان بالسالب

## المشكلة

الكارت كان بيقول:

```
تكلفتها الحقيقية:  570.57 ج
بعت الورق بـ:           0 ج
ربح الورق:        −570.57 ج   ← هامش −100%
```

السبب: مكانش فيه ولا سعر بيع محدد لأي صنف، والسيستم كان بيعتبر
"مفيش سعر" = **صفر**، فالحسبة بقت `0 − 570.57 = −570.57`.
وده غلط منطقي: انت مش بتخسر، انت لسه محددتش سعر بيع أصلاً.

## الحل — سعر الشراء هو سعر البيع الافتراضي

أي صنف من غير سعر بيع محدد بقى **سعر بيعه = سعر شرائه**، يعني ربح صفر.

```
الأصناف الجديدة  →  sell_price_kg = buy_price_kg تلقائي
الأصناف القديمة  →  اتصلّحت مرة واحدة عند الترحيل
الحركات القديمة  →  unit_sell = unit_cost
```

**النتيجة:** مفيش أي رقم بالسالب في أي مكان. الربح بيبقى صفر لحد ما تحدد سعر بيع حقيقي.

## إعادة تقييم ذكية للحركات القديمة

كان فيه تعارض: السناب شوت المفروض يمنع تغيّر الأرباح بأثر رجعي،
بس الحركات القديمة كلها اتسجلت بسعر التكلفة — يعني لو حددت الأسعار
دلوقتي، الاستهلاك القديم هيفضل ربحه صفر للأبد.

الحل قاعدة بتفرّق بين الحالتين:

| حالة الحركة | التصرف |
|---|---|
| اتسجلت بسعر بيع **حقيقي** (أعلى من التكلفة) | السناب شوت يفضل ثابت — تغيير السعر النهاردة مش هيلمس أوردر قديم |
| اتسجلت **بسعر التكلفة** (يعني لسه بدون سعر بيع) | تتعاد تقييمها بالسعر الحالي — أول ما تحدد الأسعار الأرقام القديمة تتظبط |

## تعديلات في العرض

- **حارس الخسارة** بقى يتنبّه بس لو `سعر البيع < سعر الشراء` فعلاً.
  سعر بيع = سعر شراء مبقاش يتحسب خسارة.
- **شريط تنبيه** فوق الكروت: "🏷️ ٨ صنف من ٤٤ بيتباع بسعر الشراء (ربح صفر)"
  مع أسماء الأصناف وزرار **"حدد أسعار البيع دلوقتي"** بينقلك للتبويب على طول.
- كارت الربح لما يكون صفر بيقول: **"بتبيع بسعر الشراء — حدد سعر بيع علشان تكسب"**
- عمود الربح في كل الجداول: أخضر لو موجب، رمادي لو صفر، أحمر لو سالب فعلاً.
- مصدر السعر بقى فيه حالة جديدة: **"سعر الشراء (بدون ربح)"**

## الاختبار

```
=== صنف جديد من غير سعر بيع ===
شراء 51 → بيع 51 | الربح/كجم 0                      ✅

=== الكروت (نفس حالة الصورة) ===
استهلاك: 11.53 كجم | تكلفة: 588.03 ج
بعت بـ: 588.03 ج | الربح: 0 ج | هامش: 0%            ✅ مفيش سالب
قيمة المخزون: شراء 14,711.97 | بيع 14,711.97 | ربح مخزّن 0
تنبيه: 1 صنف بيتباع بسعر الشراء

=== بعد ما نحدد سعر بيع 65 ===
بعت بـ: 749.45 ج | الربح: 161.42 ج | هامش: 27.45%   ✅
المتوقع: 11.53 × (65−51) = 161.42 ج                 ✅
ربح مخزّن في الرف: 4,038.58 ج
تحذيرات الخسارة: 0                                   ✅
```

`npm test` → ١٠/١٠ • `/system-health-check` و `/professional-health` → 200 سليم

---

# تحديث v10.5 — تعديل يومية الصنايعي في مكانها

## المطلوب

في **صفحة الورشة ← سجل الشغل في الفترة** كان فيه زرار **حذف** بس.
دلوقتي بقى فيه **تعديل** كمان، والتعديل بيتم **في مكانه على نفس السطر**.

## إزاي بيشتغل

تدوس **تعديل** → السطر نفسه بيتحوّل لخانات إدخال في مكانه (بإطار سماوي حواليه)،
وتعدّل الخانة اللي انت عايزها بالظبط:

| الخانة | نوع التعديل |
|---|---|
| التاريخ | منتقي تاريخ |
| النظام | باليومية / بالساعة |
| يوم / ساعات | رقم (اللابل بيتغير لوحده: "يوم" أو "ساعة") |
| السعر | رقم |
| إضافي | رقم |
| خصم | رقم |
| ملاحظة | نص |

**المستحق بيتحسب لحظيًا** وانت بتكتب — من غير ما تحفظ ولا تعمل ريفريش.
والصنايعي نفسه ثابت (هو اللي بيعرّف السطر).

**تبديل النظام ذكي:** لو غيّرت من "بالساعة" لـ "باليومية"، الوحدة والسعر
بيتملوا تلقائي من ملف الصنايعي (يومية ١ + أجره اليومي)، والعكس بالعكس.

الأزرار بتبقى **حفظ / إلغاء** وانت في وضع التعديل.

## الـ Endpoint الجديد

```
POST /update-workshop-attendance/:id
```

كان موجود `/save-workshop-attendance` بس بيعمل upsert بـ **(الصنايعي + التاريخ)** —
مش مناسب لتعديل سطر بعينه، خصوصًا لو التاريخ نفسه هو اللي بيتغير.
الـ endpoint الجديد بيشتغل بالـ **id** مباشرة، وبيعيد حساب المستحق على السيرفر
(مش بياخد الرقم من المتصفح)، وبيسجل التعديل في `audit_logs` بالتفاصيل:

```
تعديل يومية عبدو | التاريخ 2026-09-06 ← 2026-09-04 | المستحق 119 ← 224
```

**الحماية:** في فهرس فريد على `(worker_id, work_date)`. لو غيّرت التاريخ لتاريخ
الصنايعي عنده فيه يومية بالفعل، بيرجع رسالة مفهومة بدل خطأ قاعدة بيانات:

> `409` — عبدو عنده يومية مسجلة بالفعل في 2026-09-01

## الاختبارات

**السيرفر:**
```
تعديل 12 ساعة × 17 + 20 إضافي        → 224 ج  ✅
تغيير النظام لباليومية (1 × 200)      → 200 ج، الساعات اتصفّرت  ✅
تغيير التاريخ                         → 2026-09-04  ✅
تصادم تاريخ                           → 409 برسالة واضحة  ✅
سطر مش موجود                          → 404 "السطر مش موجود"  ✅
```

**المتصفح (Chromium فعلي):**
```
زرار التعديل ظاهر                     ✅
السطر دخل وضع التعديل في مكانه         ✅
كل الخانات اتعرضت (تاريخ/نظام/وحدة/سعر/إضافي/خصم/ملاحظة)  ✅
المستحق اللحظي 5 × 20 + 10 = 110      ✅
بعد الحفظ رجع لوضع العرض بالقيمة الجديدة  ✅
مفيش أي أخطاء في الـ console           ✅
```

`npm test` → ١٠/١٠ • `/workshop-dashboard-data` و `/system-health-check` و `/professional-health` → 200
